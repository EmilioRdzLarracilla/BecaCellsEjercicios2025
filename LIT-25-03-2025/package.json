{
  "name": "lit-25-03-2025",
  "version": "1.0.0",
  "description": "web component to show an exercise to rendering",
  "main": "script.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-dev-server --app-index index.html --node-resolve --open --watch --esbuild",
    "dev": "web-dev-server --app-index index.html --node-resolve --watch --port 3000"
  },
  "keywords": [
    "web",
    "component",
    "html",
    "lit",
    "css"
  ],
  "author": "saul.rguez.24@hotmail.com",
  "license": "ISC",
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "@web/dev-server-esbuild": "^0.4.0"
  }
}