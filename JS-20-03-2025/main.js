import { ImageCarousel, MyButton } from "./MyButton.js";

window.customElements.define('my-button', MyButton); //no se puedengeneran customElment de una sola palabra

window.customElements.define('image-carousel', ImageCarousel);

