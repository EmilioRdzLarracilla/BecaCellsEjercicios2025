let num = prompt("Digita un numero:");
let resultado = 0;

function multiplica() { 
    for (s = 1; s <= 10; s++) {
        resultado = num * i;
        console.log(`${num} * ${s} = ${resultado} `);
    }
}

multiplica();
